งานเก็บช่องโหว่ของช่องทางเดิมก่อนเปิดใช้จริง (ต่อจาก Plan 7 Phase 6) — repo /Users/seal/Documents/GitHub/AI, branch main
ทุกข้อข้างล่าง**เจ้าของตัดสินแล้ว** (2026-09-20) — ไม่ต้องถามซ้ำ ทำได้เลย; ถามเฉพาะเมื่อสำรวจแล้วพบว่าข้อตัดสินทำตามตัวอักษรไม่ได้
ตรวจ `git status -sb` ก่อนเริ่ม; commit ใหม่ห้าม push จนกว่าจะสั่ง; ห้าม rebase / force-push; ถ้า main มี commit ใหม่จาก session อื่น ให้ทำต่อบนนั้น
งานนี้มาก่อน plan/PROMPT_P7_GOLIVE_NT_REPORT.md (go-live ของ portal) — ข้อ 3 ของ prompt นั้น ("แก้ข้อบกพร่องของ REST") = งานนี้; จบแล้วแก้ prompt นั้นให้ตรง

## อ่านก่อน (บังคับ)
- plan/archive/RESULT_P7_PHASE6.md §5, §8, §9 (ของเดิมที่พบ — ต้นเรื่องของทุกข้อในงานนี้), plan/FIX_NOTES.md หัวข้อ "จาก Plan 7 Phase 6" และ "Phase 4.5"
- plan/archive/RESULT_P7_PHASE45.md §1B (ตารางที่เก็บข้อมูล S1–S13), §4–6 (retention, audit, ข้อค้าง)
- docs/manuals/manual_mcp_external.md, docs/PORTAL_INTEGRATION.md, docs/DEPLOYMENT_SECURITY.md, docs/manuals/manual_api_keys.md, mcp_servers/README.md
- code: app/api/v1/mcp_facade.py (**แบบอย่างของสิ่งที่ต้องทำกับ REST**: `_answer`, `ERRORS`, `_code_for`), app/api/v1/query.py (`run_simple_query`, `simple_query`, `_multi_response`, `list_contexts`, `contexts_for`), app/services/ai/hybrid_flow.py (:313, :776 ข้อความ 0 แถวที่มี SQL; :914–929 ข้อความ exception), app/services/multi_context.py (:320 `part.error = str(exc)`; `combine`), app/api/deps.py (`get_current_user`), app/services/api_key_service.py (`authenticate` / `validate_key` / `_check_rate_limits`), app/services/retention.py + scheduler.py (`result_retention`), app/services/query_audit.py + app/services/query_engine.py (`_audit`), app/services/user_data.py (DSR — แตะ `ai_response` อยู่แล้ว)
- ฝั่ง portal (อ่านอย่างเดียว): /Users/seal/Documents/GitHub/NT-Report/pocketbase_0/pb_hooks/assistant.pb.js — hook อ่าน field / status code / ข้อความอะไรจาก response บ้าง (การแก้ข้อ 1 ต้องไม่ทำให้ hook พัง)

## สถานะตอนนี้ (2026-09-20)
- Phase 1–6 ✅ | pytest **1035 passed, 3 skipped** — รันโดย export `CONFIG_DB_URL` / `DATABASE_URL` / `DATA_SOURCE_CACHE_DIR` ไปสำเนาเสมอ (`venv/bin/python3.14 -m pytest -q -p no:cacheprovider`; server จริงรันด้วย `venv/bin/uvicorn` = python3.10)
- **`config.db` / `app.db` จริง migrate แล้ว (2026-09-20 18:43):** เพิ่ม `data_sources.llm_data_policy` (= `full` ทั้ง 5 source) + `llm_provider_allowlist`, `workspaces.result_retention_days` + `store_result_data`; `app.db` ไม่เปลี่ยน; รันซ้ำ = ไม่มีแถวเปลี่ยน (ขยับแค่ `sqlite_sequence`). Backup ก่อน migrate: `~/nt-ai-backups/pre-migrate-20260920/` (quick_check ok)
- ยังไม่ได้ทำบนของจริง: เปิด flag `mcp_external_enabled`, ออก key จริง, start server ด้วย code ปัจจุบัน (**start ครั้งแรก job retention จะล้างผลลัพธ์เก่ากว่า 30 วัน ~1,285 แถว — เจ้าของรับแล้ว โดยมี backup ข้างบน**); Redis ยังไม่ได้รัน (เจ้าของจะรันเอง: `brew services start redis`)
- 10 commit นำหน้า origin ยังไม่ push

## งานตามลำดับ (commit เป็นก้อนต่อข้อ; ทุกข้อมี test ที่ fail บน code เดิม)
0. baseline pytest บนสำเนา (backup API + quick_check + SHA-256 ของ DB จริงก่อน/หลัง — แบบ RESULT_P7_PHASE6 §0)
1. **REST `/api/v1/query` ไม่ส่งของภายในออกไป** (เจ้าของสั่งแก้ครบ 4 ข้อ) — ใช้กับผู้เรียกของ endpoint นี้ทุกคน; **chat / telegram ไม่เปลี่ยน** (ผู้ใช้ภายในยังเห็น SQL ได้ตามเดิม):
   a. ได้ 0 แถว + `include_sql=false` → ข้อความ "ไม่พบข้อมูล" **ไม่มี SQL** (`include_sql=true` = SQL อยู่ใน field `sql` ไม่ใช่ในข้อความ)
   b. ข้อความ exception ไม่ออกใน `answer` / `error` / `parts[].error` / `parts[].answer` → รหัส + ข้อความตายตัว (ใช้คำศัพท์เดียวกับ `mcp_facade.ERRORS` — ย้ายไปที่ที่ทั้งสองใช้ร่วมได้ อย่าทำสองชุด); รายละเอียดอยู่ใน log + `query_audit.error`. **ข้อยกเว้นที่ต้องรักษา:** ข้อความที่ hook ของ portal พึ่ง (เช่น `SourceUnavailable` "กำลัง publish" — FIX_NOTES: ต้องไปถึงผู้ใช้ทั้งก้อน) → สำรวจ hook ก่อน แล้วคงไว้เป็นข้อความตายตัวที่ความหมายเดิม ไม่มี path
   c. explanation เป็น dict → คืนข้อความ `explanation` ไม่ใช่ `str(dict)` (มี `_part_answer` อยู่แล้ว)
   d. เกินโควตา = **429** ไม่ใช่ 401: แยกผลของ `validate_key` ("key ใช้ไม่ได้" vs "เกิน limit") โดย `authenticate` เดิมไม่เปลี่ยน; MCP ใช้ผลเดียวกัน (เลิกเดาว่า None หลัง authenticate = rate limit)
   → แล้วลดความซ้ำ: `mcp_facade._answer` ควรเหลือเฉพาะสิ่งที่เป็นของ MCP จริง ๆ (ทิ้งผลที่ policy ≠ full, ไม่คืน SQL แม้ขอ, cap ต่อ ask) — test ของ MCP 38 ข้อเดิมต้องผ่านโดยไม่ผ่อน
2. **`GET /api/v1/query/contexts` ต้อง auth เสมอ** (เจ้าของเลือก): ไม่มี key / session = 401; key ผิด = 401 (ไม่ตกไปเป็น "ไม่จำกัด"); key ที่ผูก workspace เห็นเฉพาะของตัวเอง; session user เห็นตามเดิม. **สำรวจก่อน:** ใครเรียก endpoint นี้โดยไม่มี auth (frontend/, frontend-admin/, telegram, NT-Report hook, OpenMiniCrew, docs) — ผู้เรียกใน repo นี้แก้ให้ส่ง auth; ผู้เรียกนอก repo เขียนเป็นข้อเสนอ; ใช้ `validate_key` หรือ `authenticate` ให้ตัดสินจากว่าการ list ควรนับโควตาไหม (ข้อเสนอ: ไม่นับ — ใช้ `authenticate`)
3. **Claude Desktop ต่อ facade ผ่าน stdio bridge + ลบ config SQL ดิบ** (เจ้าของเลือก):
   - `scripts/mcp_stdio_bridge.py`: stdio ↔ `POST /api/v1/mcp` ด้วย MCP SDK ที่ติดตั้งอยู่แล้ว (**ไม่ลง npm package ภายนอก**); key จาก env `NT_AI_API_KEY`, URL จาก `NT_AI_BASE_URL`; ส่งต่อ 3 tools ตามที่ server ประกาศ (อย่า hardcode รายชื่อ), tool error ส่งต่อทั้งก้อน, 401 = ออกด้วยข้อความที่อ่านออกบน stderr; ไม่เก็บ / ไม่ log key
   - ลบ `mcp_servers/claude_desktop_config.json`; README ของ mcp_servers: ตัดหัวข้อที่สอนต่อ nt-query เข้า Desktop, ชี้ไป bridge; ตัวอย่าง config ของ Desktop ใส่ในคู่มือ (`command` = python ของ venv, `args` = path ของ bridge, `env` = ชื่อตัวแปร — ห้ามมี key จริงในไฟล์ตัวอย่าง)
   - ทดสอบ: test ใน repo (bridge ↔ facade ผ่าน ASGI หรือ subprocess กับ server ทดสอบบนสำเนา) + **ต่อ Claude Desktop จริงบนเครื่องนี้กับ server บนสำเนา DB** — การแก้ `~/Library/Application Support/Claude/claude_desktop_config.json` เป็น config ของผู้ใช้: **เขียน snippet ให้เจ้าของวางเอง อย่าแก้ไฟล์นั้น**; บันทึกสิ่งที่ Desktop เรียกจริง (audit `channel`) ใน RESULT
   - ⚠️ ตัวเลขที่ตอบไปถึงโมเดลของ Anthropic เหมือน Claude Code — ใช้ key ทดสอบบนสำเนา
4. **`ai_response` หมดอายุตาม `result_retention_days` เดียวกัน** (เจ้าของเลือก; ไม่เพิ่ม config ใหม่): ข้อความคำตอบที่เก่ากว่ากำหนด → placeholder ตายตัว ("คำตอบหมดอายุตามนโยบายการเก็บข้อมูล — ถามใหม่ได้"); คำถาม / SQL / metadata / audit อยู่ครบ; เคารพ override ต่อ workspace และ `days=0`; idempotent. **สำรวจก่อน:** ทุกที่ที่อ่าน `ai_response` (history ที่ส่งให้ LLM เป็นบริบท follow-up, feedback / golden / auto-analyzer, export, frontend render, DSR) — placeholder ต้องไม่ถูกส่งเข้า LLM เป็น "คำตอบก่อนหน้า" และต้องไม่พัง UI; นับแถวที่จะโดนบนสำเนาสดของ `app.db` จริงแล้วรายงานตัวเลขก่อน commit. รอบแรกบนของจริงย้อนกลับไม่ได้ — backup มีแล้ว (ข้างบน) แต่ให้ทำ backup สดอีกครั้งก่อน start server จริง
5. **audit เขียนไม่ได้ = ไม่ส่งคำตอบออก เฉพาะช่องทางที่ถือ key** (เจ้าของเลือก; แนว NIST AU-5): คำขอที่มี `api_key_id` (REST ด้วย key + MCP) → ถ้าแถว `query_audit` เขียนไม่สำเร็จ ผู้เรียกได้ 503 / tool error `audit_unavailable` (ข้อความตายตัว) และ**ไม่มีข้อมูลออกไป**; chat / telegram / session user = fail-open ตามเดิม (มี `chat_history` เป็นร่องรอย) + log ERROR. `query_audit.record` ตอนนี้ "never raises" — ต้องให้ผู้เรียกรู้ผล (คืน bool หรือโหมด strict) โดยไม่เปลี่ยนพฤติกรรมของผู้เรียกเดิม; multi-context: แถวแม่หรือแถวลูกเขียนไม่ได้ = ทั้งคำถามไม่ออก; cache hit ก็ต้อง audit ได้ก่อนส่ง. ทดสอบด้วย app DB ที่เขียนไม่ได้ (read-only / ตารางหาย)
6. Review อิสระ (agent แยก อ่านอย่างเดียว) ของข้อ 1, 2, 5 — แตะสิ่งที่ REST คืนให้ผู้เรียก + auth; ข้อที่ reviewer หักล้างไม่ได้ให้ reproduce เป็น test. (workflow / subagent อาจชนโควตา session กลางทาง — ดู `<failures>` ในผล แล้วปิดช่องที่ขาดเอง)
7. ตรวจของจริงหลังแก้ (บนสำเนาสด ไม่ใช่ DB จริง): start server → source status ok ครบ → smoke 1 คำถามต่อ context ทาง REST + MCP → คำถาม 0 แถวไม่มี SQL ทั้งสองช่องทาง → `GET /query/contexts` ไม่มี auth = 401 → eval รายโดเมนไม่ลด (รันเท่าที่จำเป็น — LLM จริง)
8. อัปเดต: plan/archive/RESULT_P7_HARDENING.md (ใหม่), RESULT_P7_PHASE6 §9 (ขีดข้อที่ปิด), plan/PROMPT_P7_GOLIVE_NT_REPORT.md (สถานะ migrate + ข้อ 3 และข้อค้าง "ข้อบกพร่องของ REST" = ทำแล้ว; ตาราง status code จริงสำหรับ hook), plan/PROMPT_NT_REPORT_P7.md (ต่อท้าย: สิ่งที่ response เปลี่ยน — 429, ข้อความ error ตายตัว, `/query/contexts` ต้อง auth), FIX_NOTES, docs (PORTAL_INTEGRATION, DEPLOYMENT_SECURITY, manual_api_keys — แก้ประโยค 429 ให้ตรง, manual_mcp_external — หัวข้อ Claude Desktop), docs/changelogs; CLAUDE.md + AGENTS.md (sync กัน, gitignored); แล้วหยุด
   (ถ้า context ใกล้เต็มก่อนถึงข้อนี้ ให้หยุดแล้วเขียน prompt ส่งต่อ)

## กติกา
- พฤติกรรมที่เปลี่ยนได้ = เฉพาะที่ระบุในข้อ 1, 2, 4, 5; chat / telegram / MCP ภายในแบบ stdio / ตัวเลขที่ตอบ ต้องไม่เปลี่ยน; test เดิมที่ต้องแก้ = เฉพาะ test ที่ pin พฤติกรรมเดิมซึ่งเจ้าของสั่งเปลี่ยน (ระบุรายชื่อใน RESULT)
- ห้ามผ่อน: lock ของ DuckDB, gate `check_select`, scope / allowlist / `request_pinned` / `llm_data_policy`, `enforce_key_surface`, gate ของ MCP (ทุก request), "MCP ไม่คืน SQL"
- ของจริงแตะได้เฉพาะเมื่อเจ้าของสั่งต่อขั้น; secret (raw key, เนื้อหา `.env`, config ของ Claude Desktop) ห้ามอยู่ใน commit / log / RESULT / prompt; ตรวจ `git diff --cached` ก่อน commit
- fail closed: อ่าน key / allowlist / scope / policy ไม่ได้ = ปฏิเสธ
- ข้อควรรู้: FIX_NOTES "จาก Plan 7 Phase 6" ทั้งหัวข้อ (FastMCP ส่ง `str(exception)`; HTTP non-2xx ฆ่า session ของ MCP client; stateless ไม่ต้อง initialize; `Route` ไม่ใช่ `Mount`; `session_manager.run()` ครั้งเดียวต่อ instance → test สร้าง app ของตัวเองด้วย `mcp_facade.build()`; ORM APIKey หลุดจาก session หลัง commit; script เดี่ยวต้อง `import app.db.base` ก่อน query `User`; server ที่รันใน repo สร้าง `chroma_db__<workspace>/`); test ที่สร้าง provider จริงต้อง patch `_build_provider_kwargs`; app ห้าม import จาก scripts/; ruff มี error เดิมหลายไฟล์ที่ไม่ใช่ของเรา; dev server ให้เพิ่ม entry ชั่วคราวใน `.claude/launch.json` (gitignored) ที่ตั้ง env ไปสำเนา + `TELEGRAM_BOT_TOKEN=` แล้วลบออกเมื่อจบ
- commit เป็นก้อนต่อข้อ; ข้อความ commit ห้ามมีชื่อโมเดลหรือ emoji; ลงท้าย Co-Authored-By: Claude <noreply@anthropic.com>
- ถ้าเจอสิ่งที่ขัดกับข้อตัดสิน ให้หยุดและรายงานพร้อมทางเลือก ไม่ตัดสินใจเองเงียบ ๆ
- จบแต่ละข้อสรุป: ทำอะไร, ตัวเลข, commit list, ข้อค้าง
