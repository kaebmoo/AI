# ตรวจสถานะ F1–F8 จากโค้ด — 2026-09-21

**ขอบเขต:** แผนเดิม 10 ไฟล์ (master, F1–F8, BGE-M3) เทียบ source ที่ commit `0078dcf4c794642182581ccc4a4aa115be269595` ไม่ใช่คำสั่งให้เริ่ม waves ซ้ำ

**ผล:** มี implementation ของ F1–F8 แล้วทั้งหมด แต่คำว่า “executed” ไม่ได้แปลว่า acceptance และ operational verification ครบทุกข้อ แผน F1–F8 อยู่ใน `archive/` ส่วน BGE-M3 ยัง **PENDING DECISION** และอยู่นอก execution order

## หลักฐานรายแผน

| แผน | สิ่งที่ยืนยันจากโค้ด | การทดสอบใน repo / ข้อจำกัด |
|---|---|---|
| **F1 — implemented** | `QueryEngine._query` ใช้ `use_cache = not history` ทั้งอ่าน/เขียน; `hybrid_flow.execute_sql_attempt` อ่าน dict `truncated` และรองรับ list เดิม; `ChatRequest.provider = None` → admin default; `schema/prompt_builder.py:build_system_prompt` ใส่วันใน cache key | `test_query_cache.py`, `test_hybrid_limit_warning.py`, `test_provider_default_and_prompt_date.py` |
| **F2 — implemented ในขอบเขตเดิม** | `deps.get_admin_config_service` ปิด session ใน finally; `QueryEngine.close` ปิดเฉพาะ service ที่ตัวเองสร้าง; `_query` release dedup เมื่อ exception/result error; `chat_stream` drain queue เมื่อ task จบ และ cancel ใน finally; `query_hybrid` resolve context ก่อน retry; `retry_loop` รวม Claude tool results ใน user message; Claude ไม่ส่ง top_p คู่ temperature | `test_f2_flow_fixes.py`, `test_dedup_engine.py`; SSE test จำลอง generator ไม่ได้เรียก endpoint จริง จึงไม่ใช่หลักฐาน E2E; ข้อค้าง session นอกขอบเขตเดิมอยู่ใน `FIX_NOTES.md` |
| **F3-A — CI configuration implemented** | `.github/workflows/ci.yml`: Python 3.12, ruff critical errors, pytest unit/integration บน push main และ PR | ไม่ได้ตรวจ GitHub Actions ล่าสุด จึงยังไม่ยืนยัน “CI เขียวบน main” ในรอบนี้ |
| **F3-B — harness + baseline implemented** | `scripts/eval/run_eval.py` เปรียบเทียบผล execution; มี `eval_results/BASELINE.json` และ `.md` ที่ commit แล้ว | `test_eval_match.py`; baseline เดิม: 63 ข้อ, scored 51, exact 3, value_match 15, golden_broken 12; ไม่ใช่ accuracy ของโค้ดวันนี้ |
| **F4 — implemented** | `database_adapter.py` มี SQLite `mode=ro`, fetchmany/cap; `nt_query_mcp.validate_sql` delegate `ValidationService`; `APIKeyService` enforce per-minute ผ่าน Redis | `test_query_mcp_limit.py`, `test_api_key_rate_limit.py`; Redis ล่มยัง fail-open ตาม design; MSSQL อาศัยสิทธิ์ credential ไม่ใช่ SQLite mode=ro; ไม่ได้ทดสอบ MSSQL จริง |
| **F5 — implemented, manual E2E ค้าง** | `main.py` initialize/start/set_webhook ใน main lifespan; polling delete_webhook และเก็บ task ref; `telegram/dispatcher.py` ใช้ `/admin` explicit | `test_telegram_dispatcher.py`; `test_telegram_webhook.py` ตรวจ 404/health และ attributes ของ secret ไม่ได้พิสูจน์ webhook lifecycle จริง; ยังไม่พบหลักฐานปิด manual tunnel E2E ที่ค้างใน FIX_NOTES |
| **F6 — backend implemented, deployment ยังต้องยืนยัน** | `reports.py` ตรวจ owner/admin, enqueue Celery แล้ว fallback inline ถ้า enqueue ล้ม; `report_service.run_export` รัน saved SQL ใหม่, cap+1, Data/Info sheets, temp+rename; cleanup ถูกผูก scheduler; `_run_export_sql` route file source ตาม context แล้วด้วย | `test_report_service.py`, `test_reports_api.py`; go-live 2026-09-20 บันทึก `no such table: report_exports` บน DB จริง (`archive/RESULT_P7_GOLIVE.md`); รอบนี้ไม่ได้แตะ DB จึงยังไม่ปิด operational gap; Redis URL ไม่ได้พิสูจน์ worker พร้อม ถ้า enqueue สำเร็จแต่ไม่มี worker จะยัง pending |
| **F7 — usage/trace implemented, coverage ยังไม่ครบทุกทางออก** | ทั้ง 3 provider บันทึก `last_usage`; SQL generation อ่าน usage จริง; `ai/trace.py` สร้าง JSON; QueryEngine emit เมื่อ cache hit และเมื่อ `_execute_query` จบ | `test_token_usage.py`; dedup early return และ exception ก่อน emit ยังไม่ผ่าน trace จุดนี้ จึงไม่ควรอ้างว่าได้หนึ่งบรรทัดสำหรับทุก request; intent structured→text fallback บันทึก last_usage หลัง `_extract_once` จึงไม่ได้สะสมทุก call ที่อาจเกิดใน fallback |
| **F8 — implemented** | ทั้ง 3 provider มี `generate_structured`; `extract_intent` structured→text fallback + คืน model ใน finally; `AIService.suggest_mappings` ใช้ structured แล้ว; Gemini ใช้ JSON MIME + schema ใน prompt ตามทางเลือกที่แผนอนุญาต | `test_generate_structured.py`; code default `TWO_PASS_ENABLED=False` แต่ `archive/RESULT_F11.md` §4.1 บันทึก config จริงเปิด two-pass แล้ว จึงไม่ใช่ “ยังรอเปิด” ทั้งระบบ; รอบนี้ไม่ได้อ่าน/เปลี่ยน runtime config |
| **BGE-M3 — PENDING, ยังไม่ implement** | `VannaService.__init__` ยังส่งเพียง `config={'path': db_path}` ให้ Chroma; ไม่พบ config เลือก BGE-M3 ใน `app/config.py` | มี baseline เก่าแล้ว แต่ยังไม่มี A/B embedding evaluation หรือหลักฐานอนุมัติ; ไม่ติดตั้ง model, ไม่ re-embed, ไม่ sync brain |

เส้นทางไฟล์ในตารางอ้างอิงใต้ `app/services/`, `app/api/`, `app/providers/`, `tests/unit/` หรือ `tests/integration/` ตามชื่อที่ระบุ

## สิ่งที่เปลี่ยนจากแผนเดิม

- **MCP HTTP:** facade ภายนอกทำแล้วใน Plan 7 Phase 6 (`app/api/v1/mcp_facade.py`, stateless Streamable HTTP, flag default OFF) ส่วน `mcp_servers/*` ยังเป็น stdio ภายใน การเปลี่ยน transport ภายในเพื่อ scale ยังเป็นคนละ decision
- **F6 audit:** โค้ดปัจจุบันใช้ `query_audit.record` ทั้ง create/download แล้ว ข้อค้างเก่าที่ส่ง action/source ผิด CHECK ของ config audit ไม่ตรง source ปัจจุบัน แต่เรื่องตาราง `report_exports` บน deployment เป็นอีกประเด็นหนึ่ง
- **F4 ต่อเนื่อง:** ปัจจุบันมี DuckDB file-source query gate เพิ่มจาก SQLite hardening เดิมแล้ว ไม่ต้องย้อนกลับไป implement แผนเก่า
- **BGE prerequisite:** baseline มีอยู่จริง แต่ก่อนตัดสิน Go/No-Go ต้องเทียบ embedding บน corpus/golden/provider/config ชุดเดียวกัน และแก้/แยก golden ที่เสีย ไม่ใช้ตัวเลขปีกรกฎาคมอ้างเป็นคุณภาพปัจจุบัน

## การยืนยันและการไม่รบกวนงานปัจจุบัน

- Working tree สะอาดก่อนเริ่ม ตรวจ source และ test code แบบอ่านอย่างเดียว; เปลี่ยนเฉพาะเอกสาร ไม่แก้ source/test/config/DB, ไม่ restart, migrate, เรียก provider หรือเปลี่ยน flags
- เตรียมสำเนา commit ด้วย `git archive` ใน temporary directory ไม่สร้าง branch/worktree หรือเปลี่ยน HEAD/index ของงานเดิม
- ลองรัน selected unit tests บนสำเนาโดยใช้ DB paths แยกและ test secret: Python ทั้ง `.venv/bin/python` และ `venv/bin/python` จบก่อน collection ด้วย **`No module named pytest`**; ไม่ติดตั้ง dependency เพิ่ม ดังนั้นรอบนี้เป็น **source/test inspection ไม่ใช่ผล pytest ผ่านใหม่**
- ไม่รัน live eval, Telegram E2E, Celery worker หรือ export บน DB จริง และไม่รับรองผล CI ล่าสุด
- Checklist เดิมใน archive เป็นบันทึกตอนวางแผน ให้ใช้รายงานนี้ตีความสถานะปัจจุบัน; รายการค้างด้านบนยังต้องมีหลักฐานก่อนปิด ไม่เริ่ม coding ต่อจากการตรวจเอกสารครั้งนี้

## ตรวจต่อ 2026-09-22 (อีก session — ข้อมูลที่รอบแรกไม่ได้ตรวจ)

- **ทุกข้ออ้างในตาราง "หลักฐานรายแผน" ตรงกับ code** — ไล่ทีละข้อที่ HEAD `7cdc7af` (หลัง Plan 8.0–8.1) ยังจริงทุกข้อ รวมไฟล์ test ทั้ง 14 ที่อ้าง
- **CI บน main แดงทุกรอบตั้งแต่ 2026-09-18** (8 จาก 8 รอบ; เขียวครั้งสุดท้าย 2026-07-13) — ทุกรอบล้มตอน collection ด้วย error เดียว:
  `requirements.txt` เขียน `mcp>=1.26.0` → CI ติดตั้ง mcp 2.2.0 ซึ่งเปลี่ยนชื่อ `FastMCP` และ `streamablehttp_client` → 4 ไฟล์ test import ไม่ได้
  (`test_mcp_facade`, `test_mcp_query_identifiers`, `test_mcp_stdio_bridge`, `test_query_mcp_limit`) · ในเครื่องเป็น 1.26.0 จึงผ่าน ·
  pin `mcp==1.26.0` ใน `36d1867` — test ชุดที่เหลือไม่ได้รันบน CI ตั้งแต่ 07-13 ผลหลัง push (2026-09-23): collection ผ่าน, ล้ม 5 test ที่พึ่ง config DB ของเครื่อง dev — แก้ใน `RESULT_P8_PHASE1` §15 R2-5
- **pytest:** `.venv/bin/python` และ `venv/bin/python` (3.10) ไม่มี pytest ตามที่บันทึกไว้ข้างบน — suite ของ repo รันด้วย `venv/bin/python3.14`
  (pytest 9.0.2): HEAD `7cdc7af` ผ่าน **1180, skipped 3** (DB สำเนา)
- **F6:** DB จริงยังไม่มีตาราง `report_exports` (ตรวจแบบ `mode=ro`) — job `export_cleanup` ล้มทุกรอบบนของจริง ข้อค้างนี้ยังเปิด
