# RESULT Plan 8 Phase 8.0 — เครื่องมือที่มีอยู่กับ `feed_*` + คำถามจริงของ portal

**สถานะ (2026-09-21):** ✅ **ขึ้นของจริงแล้ว 14:51** (เจ้าของใส่ hierarchy + restart เอง — §7) · exit ผ่านบนสำเนา (คำถามจริง 4/12 → **8/12**,
legacy eval 8/37 → **8/37** ไม่มีข้อใดเปลี่ยนผล) · เจ้าของตัดสินเกณฑ์แล้ว 2026-09-21 (§8) · ยังไม่เริ่ม 8.1
**prompt:** `plan/PROMPT_P8_PHASE0_1.md` · **แผน:** `plan/PLAN_8_SELF_SERVICE_ONBOARDING.md` §5 8.0

| | ก่อน (`ca935fa`) | หลัง (`db799cc` + hierarchy ข้อเสนอบนสำเนา) |
|---|---|---|
| คำถามจริง P01–P12 ถูกครบ (ตัวเลข + ปี + ฐาน) **แบบเข้ม** | **4/12** | **8/12** (สองรอบ: 8, 8) |
| — ปี พ.ศ. + ฐานในข้อความถูก | 7/12 | **12/12** |
| คำถามจริงทั้ง 15 ข้อ (รวม 3 ข้อใหม่) | 4/15 | 10/15 |
| legacy eval (`run_eval.py --legacy`, 49 ข้อ / 37 ที่ให้คะแนนได้) | 8/37 | **8/37 — 0 ข้อเปลี่ยนผล** |
| pytest | 1092 passed, 3 skipped | **1110 passed, 3 skipped** |

---

## 0. Baseline + backup

- `git status` สะอาดบน `main` (ahead 1 = `ca935fa` ที่ยังไม่ push) · pytest บนสำเนา **1092 passed, 3 skipped** (ตรง prompt)
- backup สดของจริง: `~/nt-ai-backups/p8-phase0-20260921-112439/` — SQLite backup API จาก connection `mode=ro`,
  `quick_check = ok` ทั้งสองไฟล์, SHA-256 ของต้นฉบับก่อน/หลัง backup เท่ากัน (`config.db c67a8be4…`, `app.db 5acf882c…`)
- ตรวจซ้ำตอนจบ: ของจริงยังเป็น SHA เดิมทั้งสองไฟล์ · server จริง PID เดิมที่ port 8000 · `api_keys` จริงยังมี 4 แถว · `query_audit` จริงไม่มีแถวใหม่หลัง #280

**วิธีวัดทั้ง phase — ไม่แตะของจริง:** server ซ้อมที่ port 8001 บน**สำเนา** `config.db` / `app.db` / `chroma_db`
(`TELEGRAM_BOT_TOKEN=` ว่าง, Redis DB 1, `DATA_SOURCE_CACHE_DIR` แยก) + **key ซ้อม id 5 ที่สร้างบนสำเนาเท่านั้น**
(`p8-practice-copy`, ผูก workspace `nt-report`, 1000/นาที) · legacy eval รัน in-process บนสำเนาอีกชุด ·
restart server ซ้อมก่อนทุกรอบ (query cache 30 นาทีไม่ผูกเวอร์ชันของ code — ยิงซ้ำโดยไม่ restart = cache hit)

---

## 1. ชุดคำถามจริงใน repo (ข้อ 1) — `8b1e0b3`

`scripts/eval/portal_real_questions.json` + ตัวรัน/ให้คะแนน `scripts/eval/run_portal_questions.py` (+ test 7 ข้อ)

### 1.1 คำถาม — 15 ข้อ จากผู้ใช้จริงคนเดียว (PB user `gv7z…`)

ยืนยันกับ **PocketBase `audit_logs` action `assistant_ask`** (อ่านอย่างเดียว) — แถว `query_audit` channel `portal` มี 274 แถว
แต่ส่วนใหญ่เป็นการวัดของ session ก่อนด้วย key จริง ไม่ใช่ผู้ใช้; คำถามของผู้ใช้จริงคือแถวที่มีใน PB audit เท่านั้น:
12 ข้อของ `RESULT_F11` §7 (2026-09-20 22:33–23:00) + **3 ข้อใหม่** (2026-09-21 07:50–07:52):

| # | คำถามใหม่ | หมายเหตุ |
|---|---|---|
| P13 | รายได้ cctv รายเดือน | CCTV = 2 product (`CCTV Solution` + `CCTV on Cloud`) รวมกัน |
| P14 | ถ้าเทียบงวดเดียวกันกับปีก่อนหน้า เพิ่มขึ้นหรือลดลงเท่าไหร่ เป็นกี่ % | **ถามต่อจาก P13** แต่ `/api/v1/query` ไม่มีประวัติ → ให้คะแนนตามความหมายเมื่อถามลอย ๆ (รายได้รวม ฐานเทียบได้ตาม ⚠4) |
| P15 | …ของบริการ CCTV แบบสะสม | ผู้ใช้พิมพ์ใหม่ให้ครบเอง — สะท้อนว่า portal ไม่มีบริบทต่อเนื่อง (§9) |

### 1.2 oracle

- freeze จาก bundle ที่ publish อยู่ (`revenue` build `20260921T005851Z`, contract 2.3.2 — ข้อมูลเท่ากับ build ของ reference) ด้วย DuckDB อ่านไฟล์ตรง;
  **ทุก alternative เก็บ SQL ที่ใช้คำนวณไว้ในไฟล์** (`accept[].sql`)
- ตัวเลขตรงกับ `assistant_reference_202608.md` ทุกตัวที่มี: 3,434.07 · 26,036.32 · ex-divested ส.ค. 3,434.07 vs 3,314.22 = +3.62% · สะสม 26,036.32 vs 27,068.13 = −3.81%
- `oracle_status` ต่อข้อ: ค่าที่ `RESULT_F11` ระบุ = ใช้ค่านั้น · ข้ออื่น = "คำนวณเองจาก bundle — **รอเจ้าของข้อมูลยืนยัน**" (P13–P15 และตัวเลขรายแถวของ P03/P08–P11)
- คำถามที่ไม่ระบุเวลา (P01, P06, P09, P11, P13, P14): รับได้ทั้ง**เดือนงวดอ้างอิง**และ**สะสม** ตามเกณฑ์ ⚠3 — แต่ข้อความต้องบอกฐาน
- **P11 (การเติบโต):** เกณฑ์จัดอันดับ (ฐานเล็ก / divested) ยังไม่มีเจ้าของตัดสิน → ตรวจแค่ ชั้น product + ตัวเลขแต่ละแถวตรงกับงวดที่ใช้ + ปี

### 1.3 เกณฑ์ให้คะแนน — อ่านสิ่งที่ผู้ใช้เห็น

ผ่านเมื่อครบ 3 ข้อ: **ตัวเลขใน `data`** ตรง oracle ±0.01 ล้านบาท (บาทหรือล้านบาทก็ได้) · **ทุกปีที่ข้อความพูด** = ปีของคำตอบ (พ.ศ. = ค.ศ. + 543;
ตัวเลขจำนวนเงินและรหัสงวด `202608` ไม่นับเป็นปี) · **คำถามที่ไม่ระบุเวลา** ข้อความต้องบอกฐาน (ปี + คำบอกฐาน "สิงหาคม" / "สะสม")

**BG8 — เจ้าของตัดสิน 2026-09-21:** BG8 = `8.1 ผลตอบแทนทางการเงิน` (ดอกเบี้ยรับ) + `8.2 รายได้อื่น` (รายได้อื่น, GSaaS) ·
ภาพรวมองค์กรต้องมี BG8 · มุมกลุ่มธุรกิจไม่นับเป็น operation หลัก · **ยอดรายเดือนของรายได้อื่น / ค่าใช้จ่ายอื่นรวมเป็นยอดสะสมไม่ได้**
(ข้อมูลยืนยัน: ม.ค.+ก.พ. 2569 รายเดือน = 609.18 แต่สะสม ก.พ. = 459.24) → การจัดอันดับ (P08 / P09 / P10) รับ**มี BG8 แบบ net** หรือ**ไม่มี BG8**;
BG8 จากผลรวมรายเดือน = ผิด (`RESULT_F11` §9 เคยนับ P08 / P10 ✅ — ไม่ใช่เกณฑ์แล้ว) · ร่างแรกของไฟล์นี้มีโหมด `lenient` ไว้รอข้อตัดสิน — ลบแล้ว

**คำกำกวม — เจ้าของ 2026-09-21:** "กลุ่ม" เป็นได้ทั้งกลุ่มธุรกิจและกลุ่มบริการ, `cctv` เป็นได้ทั้ง product และกลุ่มบริการ, `internet` เป็นได้ทั้งบริการและกลุ่มบริการ —
ขึ้นกับบริบทหรือการเจาะจง → P13 / P15 รับทั้ง CCTV 2 product และกลุ่มบริการ `5.3 Cybersecurity & CCTV`

---

## 2. ปี พ.ศ. ในข้อความผิด (ข้อ 2) — `511c8ac`, `b9744fe`

### 2.1 ขั้นอธิบายผลเห็นอะไร (ไล่แล้ว)

- system prompt ของขั้นนี้ = `"You are a data visualization assistant…"` — **ไม่มีบรรทัด "ปี พ.ศ. ปัจจุบัน"** (มีเฉพาะ system prompt ของขั้นสร้าง SQL)
- user prompt (`chart_postprocessor.build_explain_prompt`) = คำถาม + SQL (ปี ค.ศ. เช่น `202608`, `year = 2026`) + แถวผลลัพธ์ (มีปีเฉพาะเมื่อ SELECT มา)
  + ตัวอย่างในกฎข้อ 2 ที่เขียนตายตัวว่า `"เดือนมกราคม 2568"` · model = `gpt-4.1-mini` (cheap tier)
- ที่ผิดจริงใน baseline: 2026 → **2568** (P01, P09 — ปีอยู่แค่ใน SQL), 2026 → **2566** (P04, P05, P13, P15), 2025 → 2565 —
  **P15 มี `"year": 2026` อยู่ในแถวที่ส่งให้ก็ยังเขียน 2566** → การแปลงปีในแถวให้เป็น พ.ศ. อย่างเดียวไม่พอ (P01/P09 ไม่มีปีในแถวเลย)

### 2.2 ที่ทำ — ตรวจข้อความใน code หลังสร้าง (`app/services/ai/thai_year.py`)

`build_explanation` → `fix_explanation`: ปีของคำตอบ = ปีในแถว + literal ในเงื่อนไขของ SQL + ปีที่ผู้ใช้พิมพ์ (`2569`, `ปี 69`, `2026`) ·
ข้อความที่ปีอยู่ในชุดนี้แล้ว = **ไม่แตะ** (ข้อที่ผู้ใช้พิมพ์ปีมาเองได้คำตอบเดิม) · ไม่งั้นเลื่อนทุกปีด้วย shift เดียวที่ทำให้ปี (distinct) ตกในชุดมากที่สุด —
การเลื่อนเป็นค่าคงที่จึงรักษาปีก่อนหน้าของการเทียบไว้ (2568 vs 2567 → 2569 vs 2568) · ไม่มี shift ที่ดีกว่าการไม่แตะ = ไม่แตะ · ใช้กับ `chart_title` ด้วย

**ข้อบกพร่องที่เจอระหว่างวัดแล้วแก้ (`b9744fe`):** รุ่นแรกนับจำนวน*ครั้ง* — คำตอบที่ถูก ("2569" 2 ครั้ง, "2568" 3 ครั้ง, SQL มีแค่ 202608)
ถูกเลื่อนเป็น "สิงหาคม 2570 เทียบ 2569" บน server ซ้อม (R4 P14) → นับเป็น*ปีที่ต่างกัน* แล้วเสมอกัน = ไม่แตะ; มี test แล้ว

**ผล:** ปี + ฐานในข้อความ 7/12 → **12/12** ทุกรอบหลังแก้ (7 รอบ) · replay บนคำตอบดิบของ baseline ได้ 12/12 เช่นกัน ·
test ที่ fail บน code เดิม 2 ข้อ (ผ่าน `build_explanation` จริง) + 4 ข้อของตัวตรวจ

---

## 3. hierarchy ของ `feed_*` (ข้อ 3) — ⏸ **รอเจ้าของตรวจ — ยังไม่ใส่ของจริง**

### 3.1 `bootstrap_from_view` บนสำเนา — ผลที่ได้

รันได้กับ file source จริง (อ่านผ่าน `source_resolver`) แต่ heuristic (**คอลัมน์ข้อความ cardinality ต่ำ เรียงจากน้อยไปมาก, สูงสุด 4 ระดับ**)
ให้ผลที่**ไม่ใช่ลำดับชั้น**สำหรับ 3 ใน 4 context:

| context / view | ระดับที่ได้ (คอลัมน์ · คำตรวจจับ · จำนวนค่า) | ตัดสิน |
|---|---|---|
| `feed_revenue` / main `fact_bu_monthly` | L0 `bu` · `["bu","bu"]` · 8 | ระดับเดียว ไม่มีคำไทย — ใช้ไม่ได้ |
| `feed_revenue` / `fact_product_monthly` | L0 `bu` (8) · L1 `service_group_seq` (31) · L2 `service_group_nt` (32) · L3 `product_name` (175, parent ว่าง) · คำ: L0 `bu` เท่านั้น; L1/L2 `กลุ่ม, กลุ่มงาน, กลุ่มบริการ` ซ้ำกัน; L3 `บริการ, ผลิตภัณฑ์` | ใช้ได้บางส่วน: รหัส+ชื่อของกลุ่มบริการกลายเป็น 2 ระดับ, "กลุ่มงาน" (= หน่วยงาน) ผิด, L0 ไม่มี "กลุ่มธุรกิจ" |
| `feed_expense` / `fact_expense` | `cost_nature` (ข/ต/บ) > `tier_code` > `tier_en` > `gl_prefix` | ❌ ไม่ใช่ลำดับชั้นของค่าใช้จ่าย |
| `feed_sales` / `fact_sales` | `metric` (actual/target) > `geo_cluster` > `geo_region_broad` > `business_group` | ❌ `metric` เป็นระดับบนสุด |
| `feed_ebt` / main `fact_ebt_division_monthly` | `division` (2) | ระดับเดียว |
| `feed_ebt` / `fact_ebt` | `partner_status` > `group_0` > `measure_type` > `division` | ❌ |

→ **ข้อเสนอ: อย่าใส่ผล auto ของ expense / sales / ebt** (ทำให้ prompt อ้างลำดับชั้นที่ไม่มีจริง) — ยังไม่เปิดใน portal จึงรอได้; ให้คนที่รู้ข้อมูลกำหนดเมื่อจะเปิด

### 3.2 ข้อเสนอสำหรับ `feed_revenue` — ตามแบบ legacy `revenue` ที่คนทำไว้ บนคอลัมน์ของ feed เอง

| ระดับ | legacy `revenue` (manual) | **ข้อเสนอ `feed_revenue`** (`source='auto'`, view `feed_revenue_fact_product_monthly`) | ค่า |
|---|---|---|---:|
| 0 กลุ่มธุรกิจ | `BUSINESS_GROUP`, `BUSINESS` · กลุ่มธุรกิจ, ธุรกิจ, business group, business | `bu`, `bu_seq` · **เหมือน legacy** | 8 |
| 1 กลุ่มบริการ | `SERVICE_GROUP` · กลุ่มบริการ, service group, **กลุ่ม** | `service_group_nt`, `service_group_seq` · กลุ่มบริการ, service group — **ตัด "กลุ่ม"** | 32 |
| 2 บริการ/ผลิตภัณฑ์ | `PRODUCT_NAME`, `PRODUCT` · ผลิตภัณฑ์, product, สินค้า, บริการ, แต่ละบริการ, รายบริการ, service | `product_name`, `product_key` · **เหมือน legacy** | 175 |

**ทำไมตัด "กลุ่ม":** เจ้าของยืนยัน (2026-09-21) ว่า "กลุ่ม" เดี่ยว ๆ เป็นได้ทั้งกลุ่มธุรกิจและกลุ่มบริการ — คำที่กำกวมจึงไม่ควรชี้ชั้นใดชั้นหนึ่ง;
คำเจาะจง ("กลุ่มธุรกิจ", "กลุ่มบริการ") ชี้ชั้น, "กลุ่ม X" ให้ค่า X ตัดสินผ่าน value lookup · legacy `revenue` / `pl_costtype` / `revenue_org` / `expense_org`
ยังมี "กลุ่ม" เดี่ยว ๆ เป็นคำตรวจจับ (แถว manual — ไม่แตะ; legacy eval ไม่เปลี่ยนแม้ประตูเปิด §6.3)

**หลักฐาน (server ซ้อม, คำถามจริง P01–P12 แบบเข้ม):** ไม่มี hierarchy 6/12 (R1, R5) · **ข้อเสนอ 8/12** (R3, R3b, R6) · ผล auto (product view) 9/12 (R4 — ต่างจากข้อเสนอแค่ P02 / P13 ซึ่งผันผวนในทุกแบบ) —
บนชุดนี้สองแบบใกล้กันเพราะทุกข้อใช้คำว่า "บริการ"; แต่ผล auto จะส่ง "กลุ่ม Mobile" / "กลุ่มธุรกิจไหน…" ไปชั้น `service_group_seq` (ตรวจ deterministic)
ขณะที่ข้อเสนอ: "กลุ่มธุรกิจไหน…" → ชั้นกลุ่มธุรกิจ, "กลุ่ม Mobile" / "Digital กับ ICT" → ไม่ชี้ชั้น (ปล่อยให้ value lookup)

### 3.3 ใส่ของจริงเมื่อได้คำสั่ง (backup สดก่อน)

```python
# จาก project root, venv/bin/python (3.10) — CONFIG_DB_URL ของจริงจาก .env
import json, sqlite3, subprocess, sys
from app.config import settings
c = sqlite3.connect(settings.CONFIG_DB_URL.replace("sqlite:///", ""))
V = "feed_revenue_fact_product_monthly"
for level, th, en, cols, kws, parent in [
    (0, "กลุ่มธุรกิจ", "Business Group", ["bu", "bu_seq"], ["กลุ่มธุรกิจ", "ธุรกิจ", "business group", "business"], None),
    (1, "กลุ่มบริการ", "Service Group", ["service_group_nt", "service_group_seq"], ["กลุ่มบริการ", "service group"], "bu"),
    (2, "บริการ/ผลิตภัณฑ์", "Product/Service", ["product_name", "product_key"],
     ["ผลิตภัณฑ์", "product", "สินค้า", "บริการ", "แต่ละบริการ", "รายบริการ", "service"], "service_group_nt")]:
    c.execute("INSERT INTO master_hierarchy (context_name, level, level_label_th, level_label_en, level_columns, "
              "detection_keywords, parent_column, source_view, source) VALUES ('feed_revenue',?,?,?,?,?,?,?,'auto')",
              (level, th, en, json.dumps(cols, ensure_ascii=False), json.dumps(kws, ensure_ascii=False), parent, V))
c.commit()
subprocess.run([sys.executable, "scripts/extract_hierarchy.py", "--context", "feed_revenue"], check=True)  # ค่า = 'auto'
```
แล้ว restart server (hierarchy cache 1 ชม.) · **ถอยกลับ:** `DELETE FROM master_hierarchy_values WHERE context_name='feed_revenue';
DELETE FROM master_hierarchy WHERE context_name='feed_revenue';` + restart (หรือ restore backup) ·
`extract_hierarchy.py` รัน migration 009 ทุกครั้ง — ตรวจแล้วมีแต่ `CREATE … IF NOT EXISTS`

### 3.4 ที่เจอสำหรับ 8.1 / 8.2

- `bootstrap_from_view` ทับ `level_columns` / `detection_keywords` ด้วย `ON CONFLICT DO UPDATE` **โดยไม่ดู `source`** — รันซ้ำบน context ที่คนแก้ไว้ = ทับของคน (ขัดหลัก 3 ของ PLAN_8) → ต้องเข้ากฎเดียวของ 8.1
- คอลัมน์ตัวเลขถูกตัดทิ้งบน DuckDB โดยบังเอิญ (`"col" != ''` กับ BIGINT = error → ข้าม) · subprocess ของ `auto_extract` log `database is locked` จาก re-sync ของ knowledge (ไม่ทำให้ extract ล้ม)

---

## 4. ประตู `if value_matches:` (ข้อ 4) — `db799cc`

**เดิม:** `detect_hierarchy_level` ทำงานเฉพาะเมื่อ value lookup เจอค่า และผลไปถึงแค่การจัดรูป "Actual Values Found" ของ pass 2 —
**pass 1 (ที่เลือก dimension เมื่อ `two_pass_enabled`) ไม่เคยเห็นระดับชั้นเลย** แม้เจอค่า

**ใหม่:** ระดับที่คำถามพูดถึงถูกตรวจจากคำในคำถามเสมอ → บรรทัด `**ระดับชั้นที่คำถามพูดถึง: <label> → คอลัมน์ <primary>**` (+ คอลัมน์รหัส, ลำดับชั้นทั้งสาย)
ใน pass 1, pass 2 และ prompt แบบ one-pass — ทุกอย่างอ่านจาก `master_hierarchy` (ไม่มีชื่อคอลัมน์ตายตัว) · ทาง value match ใช้การตรวจแบบเดิม

**regression ที่เจอแล้วแก้ในรอบเดียวกัน:** รุ่นแรกทำ legacy eval ตก **8/37 → 5/37** — legacy `expense` มี "ค่าใช้จ่าย" เป็นคำตรวจจับของชั้นรายการบัญชี
และเป็นคำที่ทุกคำถามค่าใช้จ่ายมี → "หน่วยงานที่ใช้ค่าใช้จ่ายมากสุด" / "ค่าใช้จ่ายรายฝ่าย" ถูกสั่งให้จัดตามบัญชี (#29, #32) ·
**แก้:** คำที่เป็น routing keyword ของ context (`schema_contexts.keywords` — คำที่บอก*โดเมน*) ไม่นับเป็นคำบอกระดับ + บรรทัดบอกคอลัมน์หลักคอลัมน์เดียว
(รุ่นแรกให้ทั้งชื่อและรหัส → #23 GROUP BY ทั้งสอง) → **8/37 เท่าเดิม ไม่มีข้อใดเปลี่ยนผล** · 9 ใน 16 ข้อ legacy ที่ผูก context ได้บรรทัดนี้จริง

**prompt เดิมทุกไบต์เมื่อคำถามไม่พูดถึงระดับ** — โหลด `hybrid_flow.py` ของ commit ก่อนหน้าเป็นอีกโมดูล เทียบ 4 คำถาม × one-pass / two-pass
(รวมกรณีที่มี value match): เท่ากันทุกตัวอักษรทั้ง prompt แรก, pass 1 และ pass 2 · test ที่ fail บน code เดิม 3 ข้อ + 1 ข้อ routing keyword

---

## 5. `unmatched_keywords` ของ `feed_*` 39 คำ (ข้อ 5) — ⏸ **ข้อเสนอรอเจ้าของ — ยังไม่ใส่อะไร**

ตรวจทุกคำกับค่าจริงในข้อมูลของ context นั้น (DuckDB อ่าน bundle ตรง): **ทั้ง 39 คำเป็นค่าจริงหรือส่วนของค่าจริง** — ถูก log เพราะ `feed_*`
ไม่มี hierarchy ให้ `search_aliases` เทียบ ไม่ใช่เพราะระบบไม่รู้จักคำ → เมื่อมี hierarchy (§3) กลุ่ม A/B จะหยุดถูก log เอง

| กลุ่ม | คำ (ครั้ง) | ข้อเสนอ |
|---|---|---|
| **A ค่าเต็มของคอลัมน์** (13) | revenue: `7.กลุ่มบริการอื่นไม่ใช่โทรคมนาคม` (79), `8.รายได้อื่น` (19), `1.hard infrastructure` (14), `3.mobile` (3), `102020019` (6 — `product_key`) · sales: `1.hard infrastructure` (12), `7.กลุ่ม…` (12), `3.mobile` (2) · ebt: `สายงานขายและปฏิบัติการลูกค้า 1` / `2` (33, 33) · expense: `ค่าสวัสดิการ` (17), `ค่าซ่อมแซมและบำรุงรักษาและวัสดุใช้ไป` (4), `ค่าสาธารณูปโภค` (3), `ค่าใช้จ่ายเกี่ยวกับการกำกับดูแลของ กสทช.` (3), `ค่าใช้จ่ายบริการโทรคมนาคม` (2) | **ไม่ต้องมี mapping** (value lookup / alias ของ hierarchy) |
| **B ส่วนของค่าเดียว** (8) | revenue: `hard infrastructure` (40), `1.hard` (24), `infrastructure` (2), `hard` (1), `nt broadband` (8 — ชั้น product มีค่าเดียว) · sales: `hard infrastructure` (4) · expense: `สาธารณูปโภค` (1), `กำกับดูแลของ กสทช.` (1) | ไม่ต้องมี mapping |
| **C กำกวม → ต้องตัดสินว่าหมายถึงชั้น/ค่าไหน** | ดูตารางถัดไป | |

**กลุ่ม C — ข้อเสนอ mapping ถอนแล้ว (เจ้าของ 2026-09-21):** `mobile`, `digital`, `ict`, `รายได้อื่น`, `cctv`, `broadband`, `internet`, `nt`, `solution`
(revenue) · `mobile`, `digital`, `ict solution` (sales) · `บริการโทรคมนาคม`, `โทรคมนาคม` (expense) · `สายงานขายและปฏิบัติการลูกค้า` (ebt) —
ทุกคำพบได้หลายชั้น/หลายค่า และชั้นที่ถูกขึ้นกับบริบทหรือการเจาะจงของคำถาม mapping ตายตัวไปชั้นเดียวจะผิดในอีกความหมาย →
**ไม่ใส่ semantic mapping** ให้ value lookup + คำระบุชั้นของคำถาม (§4) ตัดสิน

เหลือเรื่องเดียวที่ไม่ใช่ความกำกวมของชั้น: `ค่าใช้จ่ายตอบแทนแรงงาน` (expense) เป็นค่าเต็ม แต่ `LIKE '%…%'` จับ `ค่าใช้จ่ายตอบแทนแรงงาน-ER` ด้วย
(oracle EXP4 = `EG01_LABOR` 892.90 ไม่รวม -ER) — ตัดสินเมื่อจะเปิด expense

**ไม่กระทบคำถามจริง 15 ข้อ** (ทั้งหมดเป็นรายได้ และที่ต้องการชั้น product ได้จาก §3–§4 แล้ว) — ช่วยชุดคำถามของทีม (REV4/6/7, SAL5/6/9, EXP4/7) และ context ที่ยังไม่เปิด ·
**ยังไม่ได้ตั้ง `resolved` ของคำใดเลย** (ตาราง `unmatched_keywords` ไม่ถูกแตะ)

---

## 6. Exit 8.0 — วัดหลังข้อ 2–5 บนสำเนา

### 6.1 ทุกรอบ (server ซ้อม restart ก่อนทุกรอบ)

| รอบ | code | hierarchy `feed_revenue` ในสำเนา | P01–P12 | 15 ข้อ | ปี+ฐาน P01–12 |
|---|---|---|---|---|---|
| R0 | `ca935fa` (ก่อน) | ไม่มี | **4/12** | 4/15 | 7/12 |
| R1 | + ปี (`511c8ac`) | ไม่มี | 6/12 | 7/15 | 12/12 |
| R2 | + ปี | ข้อเสนอ | 4/12 | 6/15 | 12/12 |
| R3 / R3b | + ประตูรุ่นแรก | ข้อเสนอ | 8/12 · 8/12 | 10/15 · 10/15 | 12/12 |
| R4 | + ประตูรุ่นแรก | ผล auto (product view) | 9/12 | 10/15 | 12/12 (P14 = 2570 → `b9744fe`) |
| R5 | + ประตูรุ่นแรก | ไม่มี | 6/12 | 7/15 | 12/12 |
| **R6** | **`db799cc` (สุดท้าย)** | **ข้อเสนอ** | **8/12** | **10/15** | **12/12** |

### 6.2 รายข้อ (✅ ครบ · ปี❌ ตัวเลขถูกแต่ปี/ฐานผิด · ❌ ตัวเลขผิด) — ให้คะแนนใหม่ด้วยเกณฑ์ที่เจ้าของตัดสินแล้ว

| # | คำถาม | R0 | R1 | R2 | R3 | R3b | R4 | R5 | R6 |
|---|---|---|---|---|---|---|---|---|---|
| P01 | รายได้รวม | ปี❌ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| P02 | เอาของปีนี้สิ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ | ❌ |
| P03 | รายได้รายเดือนปี 2569 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| P04 | รายได้ บริการ NT Broadband ปีนี้ | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ | ❌ | ✅ |
| P05 | รายได้ เฉพาะบริการ NT Broadband ปีนี้ | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ | ❌ | ✅ |
| P06 | รายได้ บริการ 102020019 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| P07 | รายได้ บริการ 102020019 ปี 69 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| P08 | รายได้ ปี 69 10 อันดับแรก | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| P09 | รายได้ บริการ 10 อันดับแรก | ปี❌ | ✅ | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ |
| P10 | …รายบริการ ปี 69 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| P11 | บริการใดมีการเติบโตมากสุด | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ | ❌ | ✅ |
| P12 | ค่าใช้จ่ายมีไหม | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |
| P13 | รายได้ cctv รายเดือน | ปี❌ | ❌ | ✅ | ✅ | ✅ | ❌ | ❌ | ✅ |
| P14 | ถ้าเทียบงวดเดียวกันกับปีก่อนหน้า… | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| P15 | …ของบริการ CCTV แบบสะสม | ปี❌ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

คำตอบดิบทุกรอบ: `eval_results/portal_real_20260921_{1135,1141,1149,1154,1158,1201,1204,1209}.json` (gitignored — อยู่ในเครื่อง; ให้คะแนนใหม่ได้ด้วย `--score`)

### 6.3 legacy eval (`python -m scripts.eval.run_eval --legacy`, สำเนา)

| | exact | value match | ให้คะแนนได้ | golden_broken | รายงาน |
|---|---:|---:|---:|---:|---|
| ก่อน (code หลังข้อ 2) | 5 | 3 | **8/37** | 12 | `eval_results/eval_20260921_1155_default.*` |
| ประตูรุ่นแรก | 4 | 1 | 5/37 ❌ | 12 | `…_1203_…` (#23, #29, #32 — §4) |
| **ประตูสุดท้าย** | 5 | 3 | **8/37** — 0 ข้อเปลี่ยนผล | 12 | `…_1214_…` |

**Exit 8.0:** ✅ ถูกทั้งตัวเลขและปีในข้อความเพิ่มจาก 4/12 เป็น 8/12 — นิ่งสองรอบ · ✅ legacy eval ไม่ลด ·
เงื่อนไข: ตัวเลข 8/12 **ต้องมี hierarchy ของ `feed_revenue` ในของจริง** (§3.3) — ถ้าไม่ใส่ ของจริงจะได้ราว 6/12 (R1 / R5)

---

## 7. สถานะของจริง — ขึ้นแล้ว 2026-09-21 (เจ้าของทำเอง ตามคำสั่งที่ทดสอบกับสำเนาก่อน)

| เวลา | ขั้น | หลักฐาน |
|---|---|---|
| 14:50 | backup สด `~/nt-ai-backups/p8-deploy-20260921-145039/` (backup API จาก `mode=ro` + `quick_check`) | ไดเรกทอรีมีอยู่ |
| 14:50–14:51 | ใส่ hierarchy `feed_revenue` ตาม §3.3 | `master_hierarchy` 3 ระดับ `auto` · ค่า 8 / 32 / 175 |
| 14:51:33 | restart server port 8000 ด้วย code `8e1393f` | PID ใหม่ start 14:51:33 |
| 14:53 | คำถามจริงผ่าน portal "รายได้ บริการ 10 อันดับแรก" (`query_audit` #281) | ชั้น product · ข้อความ "สิงหาคม 2569" · ตัวเลขตรง oracle ทั้ง 10 แถว → **ผ่านทั้ง 3 เกณฑ์** (ก่อนหน้านี้ข้อนี้ได้ "สิงหาคม 2568" หรือตอบราย BU / กลุ่มบริการ) |

ยังไม่ทำ: push commit · key ของ portal (id 4) ไม่ได้แตะ ·
**ถอยกลับ:** hierarchy = `DELETE … WHERE context_name='feed_revenue'` ทั้งสองตาราง (§3.3) + restart ·
code = `git checkout ca935fa -- app/services/ai/hybrid_flow.py app/services/ai/hierarchy_context.py` + restart (ถอย code ให้ถอย hierarchy ด้วย — R2: hierarchy โดยไม่มีประตู 4/12)

## 8. ข้อตัดสินของเจ้าของ (2026-09-21) และที่ยังค้าง

| เรื่อง | ตัดสิน |
|---|---|
| คำกำกวม ("กลุ่ม", `cctv`, `internet`) | ✅ ชั้นขึ้นกับบริบท/การเจาะจง → hierarchy ไม่มี "กลุ่ม" เดี่ยว ๆ (§3.2) · ถอนข้อเสนอ semantic mapping กลุ่ม C ทั้งหมด (§5) · oracle P13 / P15 รับทั้งสองชั้น |
| BG8 | ✅ ภาพรวมต้องมี · มุมกลุ่มธุรกิจไม่นับเป็น operation หลัก · รายเดือนรวมเป็นสะสมไม่ได้ (รวมค่าใช้จ่ายอื่น) → ลบโหมด `lenient` (§1.3) |
| restart server จริง + ใส่ hierarchy `feed_revenue` | ✅ **เจ้าของทำเอง** — คำสั่งอยู่ใน §3.3 / §7 |

ยังค้าง: oracle ที่คำนวณเอง (P03/P08–P11 รายแถว, P13–P15) → NT-Report ยืนยัน · `-ER` ของ EXP4 (เมื่อจะเปิด expense) ·
เกณฑ์จัดอันดับการเติบโต (P11) · push commit · **retention ของคำตอบใน chat** (เจ้าของถาม 2026-09-21 — ข้อเสนอในแชท ยังไม่ตัดสิน)

## 9. ข้อสังเกต — ยังไม่แก้ (นอกขอบเขต 8.0 หรือรอข้อตัดสิน)

1. **P12 ("ค่าใช้จ่ายมีไหม") ขึ้นกับการมี hierarchy:** ผ่าน 3/3 รอบที่ไม่มี hierarchy ของ `feed_revenue`, ตก 5/5 รอบที่มี —
   คำตอบให้ตัวเลขรายได้พร้อมบอกว่า "ไม่มีค่าใช้จ่ายในผลลัพธ์" · prompt ของคำถามนี้ต่างกันแค่ส่วน `STEP 1.5: HIERARCHY RULE` ของ system prompt →
   การปฏิเสธคำถามข้ามโดเมนไม่มีอะไรยึดนอกจากวิจารณญาณของโมเดล (`RESULT_F11` §7 ที่ยังไม่แก้ ข้อ 3) — ต้องเป็นกฎของ context (เช่นใน contract)
2. **P02 / P08 / P10 — กับดัก BG8** (รวมรายเดือนเป็นสะสม): ความรู้ ไม่ใช่ code — ข้อตัดสิน BG8 ของเจ้าของ (§8) ควรเป็นกฎใน contract ของ NT-Report หรือ golden ของรูป "ปีนี้" ที่ใช้ `revenue_ytd`
3. **P14 — portal ไม่มีบริบทต่อเนื่อง:** ผู้ใช้ถามต่อ ("ถ้าเทียบงวดเดียวกัน…") แล้วได้คำตอบของรายได้รวม ต้องพิมพ์ใหม่เอง (P15);
   และเมื่อถามลอย ๆ ยังใช้ฐานดิบ (−305.97%) ไม่ใช่ ex-divested ตาม ⚠4 · P02 ก็เป็นคำถามต่อเนื่องเช่นกัน
4. **ค่าคงที่ใน prompt path ที่ยังเหลือ** — `prompt_builder._build_thai_prompt` บรรทัด 365–366 "ใช้ `year` และ `month` สำหรับ filter เวลา / `CAST(month AS INTEGER)`"
   (และบรรทัด 415 ของฉบับอังกฤษ) — ชนิดเดียวกับ `RESULT_F11` §8 ข้อ 1 ที่แก้ไปแล้วแต่บรรทัดนี้ยังอยู่ ขัดกับ `build_date_instructions` ของ `feed_ebt` / `feed_expense` / `pl_costtype`
   → เสนอแก้ใน 8.1 ข้อ 10 (ตรวจทุกจุดที่อ่าน 7 ตาราง) หรือแยกงาน — **ยังไม่แตะ**
5. ตัวอย่างในกฎข้อ 2 ของ prompt อธิบายผลเขียนปีตายตัว `"เดือนมกราคม 2568"` — ไม่แก้เพราะข้อ 2 ตรวจใน code แล้ว แต่เป็นค่าคงที่ใน prompt path เช่นกัน
6. P13 ผันผวน (ได้ 1 แถว = เดือนอ้างอิง แทน 20 เดือนในบางรอบ) — "รายเดือน" ถูกตีความตามกฎข้อ 3 ของ `scope_note` ("ไม่เอ่ยถึงช่วงเวลา → งวดอ้างอิง")

## commit

| commit | ข้อ |
|---|---|
| `8b1e0b3` | 1 — ชุดคำถามจริง + ตัวรัน/ให้คะแนน |
| `511c8ac` | 2 — ปี พ.ศ. ตรวจใน code |
| `b9744fe` | 2 — แก้การนับปี (distinct) ที่เจอระหว่างวัด |
| `db799cc` | 4 — ประตูระดับชั้น + `run_eval.py --legacy` |
