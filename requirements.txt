fastapi>=0.128.2
uvicorn[standard]>=0.40.0
# SQLAlchemy 2.1 reflects through pg_catalog.pg_collation, which duckdb-engine 0.17 does not have: CI got 2.1.0
# on 2026-09-25 and every DuckDB schema inspection failed (the file sources' table info). Below 2.1 until
# duckdb-engine supports it — the live server runs 2.0.25.
sqlalchemy>=2.0,<2.1
pydantic
pydantic-settings
alembic
asyncpg
psycopg2-binary
redis
celery
pyotp
jinja2
aiosmtplib
python-multipart
pandas
openpyxl
slowapi
httpx
email-validator
python-jose[cryptography]
passlib[bcrypt]
requests
google-genai
anthropic
tenacity
# mcp 2.x renamed FastMCP (mcp_servers/*, app/api/v1/mcp_facade.py) and changed streamablehttp_client: an open
# upper bound gave CI 2.2.0 and every run on main failed at collection 2026-09-18..22. Pinned to the version the
# code and the full suite run on; lift it only together with a migration to 2.x.
mcp==1.26.0

# Plan 7: file sources read in place (allowed_paths/lock_configuration verified on 1.5.5)
duckdb>=1.5
duckdb-engine>=0.17

# Integration
vanna>=2.0.2
chromadb>=1.4.1

# Testing
pytest
pytest-asyncio
pytest-cov
