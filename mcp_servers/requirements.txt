# MCP Server Dependencies
# the same pin as ../requirements.txt: mcp 2.x renamed FastMCP (from mcp.server.fastmcp import FastMCP).
# No standalone `fastmcp` package — nothing imports it
mcp==1.26.0

# Database drivers (install based on your needs)
# SQLite is built-in

# PostgreSQL
psycopg2-binary>=2.9.0

# MSSQL
pyodbc>=4.0.0

# Utilities
python-dotenv>=1.0.0
